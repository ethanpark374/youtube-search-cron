name: Innertube AI Video Pipeline

on:
  schedule:
    # KST 06:00, 12:00, 18:00, 00:00 목표 (4회/일, 지연 감안 1시간 앞당김)
    - cron: '0 20,2,8,14 * * *'
  workflow_dispatch:

jobs:
  collect:
    name: Step 1 - Innertube Keyword Search
    runs-on: ubuntu-latest
    steps:
      - name: Search all keywords (no getInfo, fast)
        run: |
          API="https://youtube-search-eight-chi.vercel.app/api/cron/collect"
          AUTH="Authorization: Bearer ${{ secrets.CRON_SECRET }}"

          KEYWORDS=(
            "KlingAI" "Runway" "LumaAI" "Seedance" "Veo" "Veo3"
            "Higgsfield" "HailuoAI" "Pika" "Wan26" "HeyGen" "Hedra"
            "Dreamina" "ViggleAI" "StableDiffusion" "ComfyUI" "PixVerse"
            "Suno" "Udio" "AICover" "AIVoiceover"
            "AIAvatar" "AIInfluencer" "AIAgent" "FacelessChannel"
            "AIGenerated" "MadeWithAI" "AIVideo" "AIGeneratedVideo"
            "AIShorts" "AIFilm" "AICinema" "AIAnimation" "AILookbook"
            "AIArtist" "TextToVideo" "ImageToVideo" "NoCamera" "SyntheticMedia" "FalAI"
            "AI%EC%98%81%EC%83%81" "AI%EC%95%A0%EB%8B%88%EB%A9%94%EC%9D%B4%EC%85%98"
            "AI%EB%A3%A9%EB%B6%81" "AI%EC%98%81%ED%99%94" "AI%EC%87%BC%EC%B8%A3" "AI%EC%88%8F%ED%8F%BC"
            "Shorts" "FYP" "AI"
          )

          echo "=== Step 1: ${#KEYWORDS[@]} keywords ==="
          for kw in "${KEYWORDS[@]}"; do
            curl -s "$API?q=$kw" -H "$AUTH" > /dev/null
            echo "  $kw"
            sleep 3
          done

  verify:
    name: Step 2 - getInfo AI Verification
    needs: collect
    runs-on: ubuntu-latest
    steps:
      - name: Check all possible videos
        run: |
          API="https://youtube-search-eight-chi.vercel.app/api/cron/recheck"
          AUTH="Authorization: Bearer ${{ secrets.CRON_SECRET }}"

          echo "=== Step 2: AI Verification ==="
          OFFSET=0
          TOTAL_UPGRADED=0
          TOTAL_CHECKED=0

          for i in $(seq 1 100); do
            RESULT=$(curl -s "$API?limit=15&offset=$OFFSET" -H "$AUTH")
            CHECKED=$(echo "$RESULT" | python3 -c "import sys,json; print(json.load(sys.stdin).get('checked',0))" 2>/dev/null || echo 0)
            UPGRADED=$(echo "$RESULT" | python3 -c "import sys,json; print(json.load(sys.stdin).get('upgraded',0))" 2>/dev/null || echo 0)
            TOTAL=$(echo "$RESULT" | python3 -c "import sys,json; print(json.load(sys.stdin).get('total',0))" 2>/dev/null || echo 0)
            NEXT=$(echo "$RESULT" | python3 -c "import sys,json; print(json.load(sys.stdin).get('nextOffset',0))" 2>/dev/null || echo 0)

            TOTAL_CHECKED=$((TOTAL_CHECKED + CHECKED))
            TOTAL_UPGRADED=$((TOTAL_UPGRADED + UPGRADED))
            OFFSET=$((NEXT - UPGRADED))

            if [ "$((i % 10))" -eq 0 ]; then
              echo "  Round $i: $TOTAL_CHECKED checked, $TOTAL_UPGRADED confirmed"
            fi

            if [ "$CHECKED" -eq 0 ] || [ "$OFFSET" -ge "$TOTAL" ]; then
              echo "  Done at round $i"
              break
            fi
            sleep 5
          done
          echo "Step 2: $TOTAL_CHECKED checked, $TOTAL_UPGRADED confirmed"

  depth:
    name: Step 3 - Channel & Related Exploration
    needs: verify
    runs-on: ubuntu-latest
    steps:
      - name: Explore confirmed channels and related videos
        run: |
          API="https://youtube-search-eight-chi.vercel.app/api/cron/depth"
          AUTH="Authorization: Bearer ${{ secrets.CRON_SECRET }}"

          echo "=== Step 3: Depth Exploration ==="
          TOTAL_SAVED=0

          for i in $(seq 1 20); do
            RESULT=$(curl -s "$API?channels=2&perChannel=8&related=5" -H "$AUTH")
            SAVED=$(echo "$RESULT" | python3 -c "import sys,json; print(json.load(sys.stdin).get('saved',0))" 2>/dev/null || echo 0)
            TOTAL_SAVED=$((TOTAL_SAVED + SAVED))
            echo "  Round $i: +$SAVED saved (total: $TOTAL_SAVED)"

            if [ "$SAVED" -eq 0 ] && [ "$i" -gt 3 ]; then
              echo "  No new discoveries. Stopping."
              break
            fi
            sleep 5
          done
          echo "Step 3: $TOTAL_SAVED additional AI videos"

  refresh:
    name: Step 4 - Stats Refresh
    needs: depth
    runs-on: ubuntu-latest
    steps:
      - name: Update views and viralScore
        run: |
          API="https://youtube-search-eight-chi.vercel.app/api/cron/refresh-stats"
          AUTH="Authorization: Bearer ${{ secrets.CRON_SECRET }}"

          echo "=== Step 4: Stats Refresh ==="
          curl -s "$API?days=3&limit=500" -H "$AUTH"
          echo ""
          echo "Done."
